<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Peni.Forums" Title="Forum">
	<StackLayout BackgroundColor="White">
		<ScrollView Orientation="Vertical" VerticalOptions="FillAndExpand">
			<ListView x:Name="ForumListView" BackgroundColor="White" RowHeight="50">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<Grid>
								<StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
									<Image Source="{Binding TopicFavorite}"/>
									<StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand">
										<Label Text="{Binding TopicName}" FontAttributes="Bold"/>
										<Label Text="{Binding TopicAuthor}"/>
									</StackLayout>
									<StackLayout Orientation="Vertical">
										<Label Text="{Binding TopicComments}"/>
										<Label Text="{Binding TopicCreationDate}"/>
									</StackLayout>
								</StackLayout>
							</Grid>
						</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</ScrollView>
		<StackLayout WidthRequest="75" HeightRequest="75" HorizontalOptions="EndAndExpand">
			<Image Source="http://i.imgur.com/eMx6VNg.png"></Image>
		</StackLayout>
	</StackLayout>
</ContentPage>
